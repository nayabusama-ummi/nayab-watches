# NAYAB Watchmaking Atelier — Frontend Environment Configuration
# Copy this file to .env and provide appropriate values.

# Backend API endpoint URL (including /api prefix)
# In development:
VITE_API_URL="http://localhost:5000/api"

# In production (e.g. deployed on separate domain or relative proxy):
# VITE_API_URL="https://api.nayab.pk/api"
